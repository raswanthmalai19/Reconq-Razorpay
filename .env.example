# Anthropic API key for the evidence-only explanation layer.
# Leave blank to run in template-fallback mode -- the reconciliation engine
# itself is entirely unaffected either way (LLM never decides a match).
ANTHROPIC_API_KEY=
